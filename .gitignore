# Build artifacts
learn
learn.exe

# Tailwind CSS standalone binary
.local/

# Tailwind CSS source (rebuilt via `make css`)
# internal/web/app.css is committed so //go:embed resolves in CI.
web/app.css

# Playwright test artifacts
.playwright-mcp/
.pi/todos/
design/
